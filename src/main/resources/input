(set-option :produce-proofs true)
(declare-const x Int)
(declare-const y Int)
(declare-sort A 0)
(declare-fun a ( ) A)
(declare-const z Int)
(assert (= x (- y 1)))
(assert (= z (+ x y)))
(assert (= z 44))
(check-sat)
(get-proof-graph)